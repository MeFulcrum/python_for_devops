# Day-06/mine — Operator examples 🔧📚

This folder contains short, self-contained example scripts based on the Day-06 notes. Each script demonstrates one family of operators and prints clear outputs you can run and inspect.

Included scripts
- `arithmetic_examples.py` — addition, subtraction, multiplication, division, floor division, modulo, power (a=10, b=5)
- `assign_op.py` — assignment operators (a starts at 10 and b=5 in each demo) ✅
- `bitwise_examples.py` — bitwise AND, OR, XOR, NOT, shifts (decimal + binary views)
- `identity_examples.py` — `is` / `is not` examples (ints and lists)
- `logical_examples.py` — `and`, `or`, `not` and short-circuit behavior
- `membership_examples.py` — `in` / `not in` with lists, sets, and strings
- `precedence_examples.py` — shows operator precedence and parentheses
- `relational_examples.py` — comparison operators: ==, !=, <, >, <=, >=

How to run
1. Open a terminal in `Day-06/mine`
2. Run one of the scripts, for example:

```bash
python arithmetic_examples.py
python assign_op.py
python bitwise_examples.py
python identity_examples.py
python logical_examples.py
python membership_examples.py
python precedence_examples.py
python relational_examples.py
```

Notes & tips
- Most scripts use simple fixed values (often `a=10` and `b=5`) so outputs are deterministic and easy to read.
- Use `python <script> --help` if a script supports CLI options (currently most are simple run-and-see demos).
- Feel free to modify the constants in each script to experiment with different values.

Enjoy experimenting! ✅
