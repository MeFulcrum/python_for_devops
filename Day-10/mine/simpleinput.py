
user_input = input("enter two numbers : ")

num_arr = user_input.split()

num_total1 = num_arr[0] + num_arr[1]

print(" total is of num_total1 : ", num_total1)

num_total2 = int(num_arr[0]) + int(num_arr[1])

print(" total is of num_total2 : ", num_total2)

num_total3 = float(num_arr[0]) + float(num_arr[1])

print(" total is of num_total3 : ", num_total3)